summary:
  per_split: false
  split_columns: false
  partitions: 256

privacy:
  min_subjects: 20
  min_split_subjects: 1
  rare_code_action: bucket
  rare_code_label: __RARE__
  round_counts_to: 5

# Optional local Athena source:
# enrichment:
#   csv_dir: /path/to/athena
#   parent_codes: true
#   child_codes: false
#   sibling_codes: false
#   child_depth: 3
# Use `postgres: postgresql://postgres@localhost/omop` instead for PostgreSQL.
